# Flask 博客系统依赖包列表
#
# 安装方式一: pip
#   pip install -r requirements.txt
#
# 安装方式二: conda
#   conda create -n flask_blog python=3.9
#   conda activate flask_blog
#   pip install -r requirements.txt
#
# 或者使用 environment.yml: conda env create -f environment.yml

# Flask 核心框架
Flask==2.3.3

# Flask 数据库扩展
Flask-SQLAlchemy==3.0.5

# Werkzeug 安全工具
Werkzeug==2.3.7

# Gunicorn 生产环境 WSGI 服务器（可选）
gunicorn==21.2.0

# Python 环境变量管理（可选）
python-dotenv==1.0.0
